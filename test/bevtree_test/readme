
使用chrome gliffy diagrams绘制

绘制规则：
1. 基本shape代表一个节点
2. 基本line代表关联关系

节点的命名规范：
1. 最长64字节,包括结束\0
2. 命名分三段：类型|描述|回调ID 如果没有回调分两段即可
    类型说明:
        "SEQ" : sequence
        "SEL" : selector
        "COND" : condition
        "ACT" : action
        "PAR" : parallel (default all)
        "PAR_ALL" : parallel all ok is ok
        "PAR_ONE" : parallel one ok is ok
    回调ID：需要在代码中注册实现

